#! /usr/bin/env bash

# Fail fast
set -o errexit

if [ -z "$1" ]; then
  mvim -v $(git ls-files | grep -v "^vendor" | selecta)
else
  mvim -v $(git ls-files | grep -v "^vendor" | selecta --search "$1")
fi
