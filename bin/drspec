#! /usr/bin/env bash

files=`git diff master --diff-filter=d --relative --name-only ./spec | grep "_spec\.rb"`

if [ -z "$files" ] ; then
  echo "No changes detected"
else
  echo $files | xargs bundle exec rspec $@
fi
