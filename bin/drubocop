#! /usr/bin/env bash

files=`git diff master --diff-filter=d --relative --name-only . | grep "\.rb"`

if [ -z "$files" ] ; then
  echo "No changes detected"
else
  echo $files | xargs bundle exec rubocop $@
fi
