#! /usr/bin/env bash

cd ~/Code

if [ -z "$1" ]; then
  cd $(find . -maxdepth 3 -type d -name .git | sed "s/\/\.git$//" | selecta)
else
  cd $( find . -maxdepth 3 -type d -name .git | sed "s/\/\.git$//" | selecta --search "$1")
fi
