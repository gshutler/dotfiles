#! /usr/bin/env bash

# Fail fast
set -o errexit

cd ~/Code

if [ -z "$1" ]; then
  cd $(find . cronofy evenitron gshutler zopa -maxdepth 1 -type d | selecta)
else
  cd $(find . cronofy evenitron gshutler zopa -maxdepth 1 -type d | selecta --search "$1")
fi
