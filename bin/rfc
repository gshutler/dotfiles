#! /usr/bin/env sh

# Fail fast
set -o errexit

cd ~/RFCs

rfcs=`find . -maxdepth 1 -type f -print | sed 's,./,,'`

if [ -z "$1" ]; then
  $EDITOR $(echo "$rfcs" | selecta)
else
  $EDITOR $(echo "$rfcs" | selecta --search "$1")
fi
